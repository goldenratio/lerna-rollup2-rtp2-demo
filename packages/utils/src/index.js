/**
 * @param {number} x
 * @param {number} y
 * @returns {number}
 */
export const add = (x, y) => x + y;

/**
 * @param {number} x
 * @param {number} y
 * @returns {number}
 */
export const sub = (x, y) => x - y;
